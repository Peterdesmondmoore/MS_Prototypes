# Prototype review notes

`mobile-sample` and `laptop-sample` are the protected Mission Surface baseline samples (`isSample: true`), not product prototypes. Use them to verify ingestion and review behavior. Do not update their manifests, experiences or artifacts as a side effect of other repository work.

Document the experience hypothesis, walkthrough, page identifiers, stakeholder questions and known limitations for both delivery modes here.

Both baseline samples demonstrate the same high-level path: download the starter, connect GitHub, enable GitHub Pages for a live prototype or run `prepare-and-validate.ps1` for images, verify the revision, review and comment in Mission Surface, and submit the simulated outcome to the Request dashboard.

Confirm the live example completes its bridge handshake and posts exact manifest pages. Confirm the screenshot example maps every page exactly once, creates no public bundle import and remains understandable with Mission Surface-owned navigation, Explain, fullscreen and comments.
